# Copy this file to .env and set your values before running tests via Docker.
#
#   cp .env.example .env

# Required — URL of the Magento site with the CloudFlare FPC Worker deployed.
# Must include a trailing slash.
# Example: https://www.mystore.com/
TEST_URL=https://yoursite.com/
